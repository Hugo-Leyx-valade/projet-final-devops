services:
  database:
    image: postgres:latest
    container_name: efrei_devops_db
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_USER=postgres
      - POSTGRES_DB=efrei_devops
    networks:
      - db

networks:
  db:
