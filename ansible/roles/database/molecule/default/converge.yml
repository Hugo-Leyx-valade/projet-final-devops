---
- name: Converge
  hosts: all
  become: true
  vars:
    app_user: appuser
    db_host: 127.0.0.1
    db_port: 5432
    db_name: appdb
    db_user: appuser
    db_password: testpassword
  roles:
    - role: database
