---
# Configuration de la base de données RDS (non-sensible)
db_port: 5432
db_name: appdb
db_user: appuser

# Valeurs sensibles provenant du vault (chiffré)
db_host: "{{ vault_db_host }}"
db_password: "{{ vault_db_password }}"
